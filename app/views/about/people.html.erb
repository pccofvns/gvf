 <% provide(:title, "People") %> 
 <!-- Page Content -->
<div class="container">
    <!-- Team Members Row -->
    <div class="row">
        <div class="col-lg-12">
            <h2 class="page-header"><i class="fa fa-users"></i>&nbsp;Our Team</h2>
        </div>
        <div id="peopleContainer">
            <%= render 'layouts/governingCouncil' %>
            <%= render 'layouts/advisors' %>
            <%= render 'layouts/members' %>
            <%= render 'layouts/researchers' %>
            <%= render 'layouts/interns' %>

        </div>
    </div>

    <hr>
</div>
<!-- /.container -->