<div class="row" id="govCouncilDiv">
	<h3 class="page-header"><i class="fa fa-users"></i>&nbsp;Governing Council</h3>
	<% @governingCouncil.each_with_index do |person, index| %>
		<div class="col-md-4 col-lg-4 text-center top10">
			<% if Rails.application.assets.find_asset('people/'+person[:id]+'.jpg') != nil %>
		    	<img src="<%= asset_path('people/'+person[:id]+'.jpg') %>" class="img-circle img-responsive img-center center-block">
		    <% else %>
		    	 <img src="<%= asset_path('200x200.png') %>" class="img-circle img-responsive img-center center-block">
		    <% end %>
		    <h3><%= person[:name] %></h3>
		        <span class="text-nowrap"><small><%= person[:position] %></small></span>
		</div>
	<% end %>
</div>